---
apiVersion: v1
kind: Service
metadata:
  name: ai-gpu-flask
  namespace: ai-inference
  labels:
    app: ai-gpu-flask
spec:
  selector:
    app: ai-gpu-flask
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
